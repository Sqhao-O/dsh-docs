- insert:
    - id: dsh-doc
      name: dsh-doc
      config:
        # Extra directories whose documents the model may read, beyond the
        # session workspace (which is authorized implicitly unless
        # allowWorkspaceFiles is set to false).
        allowedLocalRoots: []
