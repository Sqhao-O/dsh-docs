- insert:
    - id: dsh-docling
      name: dsh-docling
      config:
        # Set this to the directories whose documents the model may read.
        # An empty list keeps local-file conversion disabled by default.
        allowedLocalRoots: []
